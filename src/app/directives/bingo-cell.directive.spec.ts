import { BingoCellDirective } from './bingo-cell.directive';

describe('BingoCellDirective', () => {
  it('should create an instance', () => {
    const directive = new BingoCellDirective();
    expect(directive).toBeTruthy();
  });
});
